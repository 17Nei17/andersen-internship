// Создайте функцию, принимающую на входе целое число и возвращающую количество цифр  в этом числе.

function givNumeral(x) {
  const numeral = String(x);
  console.log(`${numeral.length}`);
}
givNumeral(325443);
